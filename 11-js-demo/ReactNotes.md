UI framework
    -UI = f(state)
    -Declarative
    -Custom "components
        -reusable
        -using JSX
            -is *Transpiled* into javascript
    -Data binding (useState)

Vite:
    -Transpoles JSX using ESBuild (or Babel)
    -"Bundles" -> puts all code into a single file
    -Providees a server
    -Provides initialization tools

npm create vite@latest
    -> React
        ->JavaScript
            ->No
                ->Yes
npm run dev restarts the server